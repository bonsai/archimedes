[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "archimedes-agent"
version = "0.1.0"
description = "Repository architecture and self-organization agent for bonsai"
requires-python = ">=3.11"

[project.scripts]
archimedes = "archimedes.cli:main"
